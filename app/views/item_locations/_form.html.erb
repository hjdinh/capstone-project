<%= form_for(@item_location) do |f| %>
  <% if @item_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_location.errors.count, "error") %> prohibited this item_location from being saved:</h2>

      <ul>
      <% @item_location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.collection_select :user_id, User.where(:name => current_user.name), :id, :name %>
  </div>

  <div class="field">
    <%= f.label :location_type %><br>
    <%= f.select(:location_type, options_for_select(['Movable', 'Off-Campus'], 3), {}, class: "chzn-select", style: "width: 100px") %>
  </div>

  <div class="field">
    <%= f.label :location_address %><br>
    <%= f.text_field :location_address, style: "width: 700px" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
