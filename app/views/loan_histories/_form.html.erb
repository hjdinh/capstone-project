<%= form_for(@loan_history) do |f| %>
  <% if @loan_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@loan_history.errors.count, "error") %> prohibited this loan_history from being saved:</h2>

      <ul>
      <% @loan_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :check_in_date %><br>
    <%= f.datetime_select :check_in_date %>
  </div>
  <div class="field">
    <%= f.label :check_out_date %><br>
    <%= f.datetime_select :check_out_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
