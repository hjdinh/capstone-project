<%= form_for(@tag_status) do |f| %>
  <% if @tag_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_status.errors.count, "error") %> prohibited this tag_status from being saved:</h2>

      <ul>
      <% @tag_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_status %><br>
    <%= f.text_field :tag_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
